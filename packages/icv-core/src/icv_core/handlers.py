"""
Signal handlers for icv-core.

Connected in IcvCoreConfig.ready(). This module is imported at startup;
keep imports lazy where possible to avoid circular imports.
"""

# Placeholder — handlers will be added here as features are implemented.
# Example pattern:
#
# from django.db.models.signals import post_save
# from django.dispatch import receiver
#
# @receiver(post_save, sender=SomeModel)
# def handle_some_model_save(sender, instance, created, **kwargs):
#     ...
